# Mini App

Next.js shell for the Telegram Mini App experience.

The Mini App now contains:

- a real Next.js App Router shell
- shared UI primitives from `@corens/ui`
- MVP routes for home, profile, state/intent, trust keys, beacon, connection, consent, privacy, and delete
- view models derived from `@corens/domain` policy helpers while public API routes are still being finalized
