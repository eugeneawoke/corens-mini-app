export const beaconModule = {
  name: "beacon",
  responsibilities: [
    "activate fixed-duration Beacon sessions",
    "enforce cooldown and daily limits",
    "expose Beacon status"
  ]
} as const;
