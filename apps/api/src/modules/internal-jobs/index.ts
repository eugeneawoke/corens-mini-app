export const internalJobsModule = {
  name: "internal-jobs",
  responsibilities: [
    "coordinate in-process maintenance hooks",
    "schedule lightweight sweeps",
    "keep orchestration contracts stable inside the combined backend runtime"
  ]
} as const;
