{
  "name": "@corens/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "echo 'No dedicated lint task is configured for @corens/api'",
    "start": "prisma db push --skip-generate --schema ../../packages/db/prisma/schema.prisma && node dist/main.js",
    "test": "echo 'No tests are configured for @corens/api'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@corens/config": "workspace:*",
    "@corens/db": "workspace:*",
    "@corens/domain": "workspace:*",
    "@corens/telegram": "workspace:*",
    "@nestjs/common": "^11.1.16",
    "@nestjs/config": "^4.0.3",
    "@nestjs/core": "^11.1.16",
    "@nestjs/platform-express": "^11.1.16",
    "express": "^5.2.1",
    "grammy": "^1.41.1",
    "pg": "^8.16.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "@types/express": "^5.0.6"
  }
}
